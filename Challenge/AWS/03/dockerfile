# FROM -> 이미지 생성 시 기반이 되는 이미지 레이어
# <이미지 이름> : <태그> 형식으로 작성
# -> 태그 미작성 시 자동으로 최신 것으로 다운 받는다.

# 베이스 이미지 명시하기
# alpine -> 용량 사이즈가 작은 베이스 이미지
FROM alpine


# RUN -> 도커 이미지가 생성되기 전에 수행할 셸 명령어

# 추가적으로 필요한 파일들을 다운 받기
# RUN command
# 왜 주석? = hello 문자를 출력하기 위한 추가적인 파일이(echo) 이미 베이스 이미지에 존재


# CMD -> 컨테이너가 시작되었을 때 실행할 실행 파일 또는 셸 스크립트
# 한 도커파일 내에 1회만 사용 가능

# 컨테이너 시작 시 실행 될 명령어 명시하기
CMD [ "echo", "hello" ]